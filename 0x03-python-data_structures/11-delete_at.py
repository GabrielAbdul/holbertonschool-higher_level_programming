#!/usr/bin/python3


def delete_at(my_list=[], idx=0):
    if not my_list
        return my_list
    list_len = len(my_list)
    if idx > list_len:
        return my_list
    my_list[idx:idx + 1] = []
    return my_list
